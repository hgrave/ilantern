#!/usr/bin/env bash
# Run a Nextcloud occ command inside the app container, e.g.
#   ./scripts/occ user:list
#   ./scripts/occ config:system:set trusted_domains 1 --value=cloud.example.com
set -euo pipefail
cd "$(dirname "$0")/.."
exec docker compose exec -T --user www-data app php occ "$@"
